services:
  - type: web
    name: amazon-ads-backend
    env: python
    plan: free
    region: oregon
    buildCommand: |
      python -m pip install --upgrade pip setuptools wheel
      pip install --no-cache-dir -r requirements.txt
    startCommand: ./start.sh
    autoDeploy: true
